## Restore NeonFlow Studio

I'll restore your downloaded NeonFlow Studio project into this workspace.

### What's in the zip (125 files)
- Full TanStack Start app (router, routes, components, UI library)
- Authenticated app routes: dashboard, projects, orders, employees, customers, rates, payroll, approvals, reports, users, owner tools (sync, prices, attendance QR), and "me" routes (jobs, earnings, attendance, scan)
- Auth route + `_authenticated` route gate
- Supabase integration files (client, server client, auth middleware, types)
- 18 Supabase migrations under `supabase/migrations/`
- Server helpers (sheet-sync, orders, sync functions, attendance token)
- Public webhook route `api/public/hooks/sync-projects`
- Config: package.json, bun.lock, vite.config.ts, tsconfig.json, components.json, eslint, prettier, styles.css

### Steps

1. **Enable Lovable Cloud** in this project (required — the app uses Supabase auth, RLS, server functions, and a typed DB schema). Cloud will provision a fresh backend and inject env vars.
2. **Extract the zip** to `/tmp/neonflow` and verify there's no `.git` directory inside.
3. **Copy source into the project** with `rsync`, excluding:
   - `.git/**` (safety)
   - `.env`, `.lovable/**`, `bun.lock` (workspace-managed; Cloud writes its own `.env`, and bun will regenerate the lockfile)
   - `src/integrations/supabase/client.ts`, `client.server.ts`, `auth-attacher.ts`, `auth-middleware.ts` (Cloud-managed; use the fresh ones generated when Cloud is enabled)
   - `src/routeTree.gen.ts` (auto-generated by the Vite plugin)
4. **Install dependencies** from the restored `package.json` (`bun install`).
5. **Apply the 18 Supabase migrations** to the new Cloud database in chronological order so tables, RLS policies, functions, and seed structures match the original.
6. **Reconcile `src/integrations/supabase/types.ts`** — keep the restored types if they match the migrations; otherwise regenerate after migrations apply.
7. **Verify build** — let the harness build, fix any import/path issues that surface (likely small, since the zip is a complete working snapshot).
8. **Smoke test** the preview: load `/auth`, sign up a first user, confirm the `_authenticated` gate redirects properly and the dashboard renders.

### Things you'll need to redo manually after restore
- **Secrets**: the original `.env` is skipped. If the app used any custom secrets (e.g., Google Sheets sync keys, webhook secrets), you'll need to re-add them via the secrets panel. I'll list what's referenced in code after extraction.
- **First user / roles**: the new database starts empty. You'll sign up fresh and I can grant your account the `owner`/`admin` role via SQL.
- **Google Sheets / external integrations**: any external service that was pointed at the old project's URL will need to be repointed at this project's URLs.

### Out of scope for this restore
- I won't restore old user accounts or business data — only schema and code. The DB starts empty.
- I won't reconnect any third-party services automatically; you'll tell me which ones and provide keys.

Approve and I'll enable Cloud and run the restore.